version: "3"

services:
  portfolio:
    build: .
    ports:
      - "3000:3000"

  socket-server:
    image: node:20
    working_dir: /app
    volumes:
      - .:/app
    command: node server/server.js
    ports:
      - "3001:3001"